package com.fatihenes.photoreport.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Minimalist & Modern Premium Palette - Refined Contrast
 */

// Primary Accent (Deep Indigo / Sapphire)
val Primary = Color(0xFF4F46E5) // Indigo 600
val PrimaryContainer = Color(0xFFE0E7FF) // Indigo 100
val PrimaryDark = Color(0xFF3730A3) // Indigo 800

// Secondary
val Secondary = Color(0xFF64748B) // Slate 500

// Backgrounds - Slate tinted
val BackgroundLight = Color(0xFFF8FAFC) // Slate 50
val BackgroundDark = Color(0xFF020617) // Slate 950 (Deep OLED Black/Slate)

// Surface
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF0F172A) // Slate 900

// Surface Variant
val SurfaceVariantLight = Color(0xFFF1F5F9) // Slate 100
val SurfaceVariantDark = Color(0xFF1E293B) // Slate 800

// Semantic
val Error = Color(0xFFEF4444) // Red 500
val Success = Color(0xFF10B981) // Emerald 500

// Legacy compatibility & Text
val OnBackground = Color(0xFF0F172A) // Slate 900
val OnBackgroundSecondary = Color(0xFF334155) // Slate 700
val OnBackgroundTertiary = Color(0xFF94A3B8) // Slate 400
val Border = Color(0xFFE2E8F0) // Slate 200
val BorderLight = Color(0xFFF1F5F9) // Slate 100

package com.fatihenes.photoreport.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Minimalist & Modern Premium Palette - Refined Contrast
 */

// Primary Accent (Deep Royal Blue)
val Primary = Color(0xFF007AFF)
val PrimaryContainer = Color(0xFFE5F1FF)
val PrimaryDark = Color(0xFF0056B3)

// Secondary
val Secondary = Color(0xFF8E8E93)

// Backgrounds - Stronger contrast for light mode
val BackgroundLight = Color(0xFFF8F9FA) // Cleaner, slightly cooler white
val BackgroundDark = Color(0xFF000000)

// Surface - Pure white surfaces on off-white background
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1C1C1E)

// Surface Variant - More distinct for area definition
val SurfaceVariantLight = Color(0xFFF1F2F6) // Noticeably darker than pure white
val SurfaceVariantDark = Color(0xFF2C2C2E)

// Semantic
val Error = Color(0xFFFF3B30)
val Success = Color(0xFF34C759)

// Legacy compatibility & Text
val OnBackground = Color(0xFF1C1C1E)
val OnBackgroundSecondary = Color(0xFF48484A)
val OnBackgroundTertiary = Color(0xFF8E8E93)
val Border = Color(0xFFE5E5EA) // Distinct borders
val BorderLight = Color(0xFFF2F2F7)
